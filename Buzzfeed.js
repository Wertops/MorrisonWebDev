
function(makeResult);
{
  var qstns;
  //might have to change the two below tagnames to ids. I'm not exactly sure
  var resultnumber = document.getElementbyTagName("resultno")
  var questionnumber = document.getElementbyTageName("questionno")
  // if you feel like it and have time, you can add the "add question button again. var numItems = $('.item').length; will make a variable to count the number of classes
  for (qstns = 1; qstns <= questionno; qstns++){
   //here I am trying to 
   if (document.getElementByID(
}
function(makeAnother);
{
  
}
